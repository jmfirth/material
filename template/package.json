{
  "name": "preact-cli-template-material",
  "version": "1.0.0",
  "main": "src/index.tsx",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && yarn run -s serve || yarn run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "test": "eslint src && preact test"
  },
  "eslintIgnore": ["build/*"],
  "devDependencies": {
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.1",
    "preact-cli-plugin-typescript": "^0.2.2",
    "tslint": "^5.7.0",
    "tslint-config-airbnb": "^5.3.0"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.5",
    "preact-material-components": "^1.1.6"
  }
}
